---

extraction: 
    python:
        after_prepare:
            - export PATH=$LGTM_WORKSPACE/tools:$PATH
            - echo hellworld >> /opt/out/snapshot/log/build.log
            - echo helloworld
